#include "NewFeature.h"

NewFeature::NewFeature(BlockSource *region, BlockPos const&pos):Feature(entity)
{
	//for(int i=0;i<=10;i++){
		//_setBlockAndData(*region, {pos.x,pos.y+i,pos.z}, FullBlock(7,0));
	//}
}
